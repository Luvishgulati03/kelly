// Moved to ../embeddings.ts (shared between knowledge base and personal memory).
// Re-exported here so any existing importers of this path keep working.
export { LocalEmbeddingProvider } from "../embeddings.ts";
